apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  type: LoadBalancer
  selector:
    app: gateway
  ports:
    - name: "http-port"
      protocol: "TCP"
      targetPort: 50002
      port: 10000